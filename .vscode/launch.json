{
  "version": "0.2.0",
  "configurations": [{
    "name": "Launch Character Relationship Chart",
    "type": "extensionHost",
    "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
    "outFiles": ["${workspaceFolder}/src/**/*.js"]
  }]
}
