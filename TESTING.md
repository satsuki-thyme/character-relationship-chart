# 検証結果と実機確認手順

## Web版転用・第1段階（2026-09-23 UTC）

対象: 0.1.0を基にしたUI/VS Code境界分離版。拡張ID・表示名・版数、データ形式は変更していない。作業用コピーで実装・検証し、更新ファイルとVSIXを生成。以下は直接反映前に取得した検証結果。commit/push、公開は行っていない。Dropboxへの反映記録は [stage1-applied.md](dist/character-relationship-chart-stage1-applied.md) に記載する。

環境: Linux、Node.js v24.19.0、npm 11.9.0。Dropboxの現行ソースはダウンロード時にサイズと内容ハッシュを検証して取得した。

| 項目 | 今回の結果 |
| --- | --- |
| 変更前の基準 | 既存54テスト成功 |
| 依存導入 | lockfileを維持して `npm ci --ignore-scripts` 成功 |
| 構文 | `npm run check` 成功。本番JavaScript10ファイル |
| 通常テスト | `npm test` 61件成功（既存54＋境界7）、失敗・スキップなし |
| 境界 | API取得・旧状態消去をアダプターへ隔離。通信順、購読解除、非同期応答の対応付け、破棄時の未完了要求を検証 |
| 拡張との接続 | 実アダプター＋実extension.js＋模擬VS Code APIでGUI編集・人物ID変更と配置引継ぎ・版競合・保存失敗・SVG保存を検証 |
| DOM検証 | jsdom 26.1.0の追加7件成功。VS Code APIなしのホスト注入、描画・編集、コメント保持、配置保存/再読込、設定を開く操作、SVG生成、入力保持、古い保存応答、新しい外部変更、不正設定・不正メッセージ・HTML文字列を確認 |
| 依存関係 | 本番の依存・lockfileは変更なし。jsdomは検証用の別フォルダへ導入し、VSIXに含めない |
| VSIX | `npm run package` 成功。共有HTML、新しいアダプター・起動スクリプト・テーマCSS、jsonc-parserとLICENSEを含む。開発テスト・管理文書・バックアップを除外 |
| 維持した処理 | `src/extension.js`、`storage.js`、`config.js`、`edit.js`、`media/graph.js`、Schema、サンプルは取得時から変更していない |

実ブラウザによる画面確認は、提供されたブラウザがローカル検証URLを `ERR_BLOCKED_BY_CLIENT` で拒否したため未実施。ローカルChromiumの起動試行もプロセスが終了した。DOM検証では画面サイズ・ResizeObserver・ダイアログ・一部描画タイミングを模擬しているため、見た目、ドラッグの実操作、明暗テーマの色、CSPの実ブラウザでの適用を確認したとは扱わない。

実際のVS Code本体がないため、VSIX実インストール、Extension Host、Undo、OS別動作は未確認。末尾の実機確認手順を継続して使う。独立Web製品版は未実装。検証用画面とDOM検証の再実行方法は [開発ガイド](docs/DEVELOPMENT.md) を参照。

## 以下は公開準備時の記録（今回の再実行結果とは別）

対象: Character Relationship Chart 0.1.0（2026-09-23）。Linux、Node.js v24.19.0、npm 11.9.0で確認。

## この版で確認したこと

| 項目 | 結果・範囲 |
| --- | --- |
| クリーン導入 | 公開対象31ファイルのみを別ディレクトリへ展開し、既存node_modulesやGitメタデータなしで `npm ci` →構文検査→54テスト→VSIX作成に成功 |
| JavaScript構文 | `npm run check` 成功（src／mediaの8ファイル） |
| 既存の自動テスト | `npm test` 54件成功、失敗・スキップなし |
| テスト内訳 | 設定14、JSONC編集7、VS Code模擬API12、描画計算12、保存9 |
| 実ファイルの読み書き | 別の一時フォルダへサンプルを保存し、人物ID・複数所属の変更、関係参照の追従、コメント保持を確認 |
| 配置データの保存 | 本番のcreateStorageへ実ファイルAPIを渡し、書込・再読込・別名／別フォルダへの移動後の復元を確認 |
| 保存競合 | 外部で書き換えた表示ファイルを保護し、古い状態での上書きを拒否 |
| サンプルと描画計算 | 6人物・4グループ・8関係の読込と8本の経路計算が成功 |
| VSIX | `npm run package` 成功。実行時依存・サンプル・Schema・GUIスクリプト・第三者LICENSEを同梱 |
| 公開範囲 | 整備用バックアップは新形式 `backup-YYYY-MM-DD-NN` に統一し、新旧両形式がGitで除外。VSIXにもバックアップ・verification・開発テスト・個人用フォルダなし |
| 内容の確認 | 公開対象ソースと新しいソースZIP・VSIXの内容・ファイル名に、廃止した個人用名称がないことを大文字小文字を区別せず確認 |
| Git履歴 | 初回公開準備時の調査では初回コミット前。今回もcommit・pushは実行していない |

VS Code模擬APIのテストは実際のExtension HostやWindowsのGUIを実行するテストではありません。描画計算の成功も画面の見た目を保証するものではありません。

## バックアップ規則更新の確認

- 既存13バックアップは内容を変えずに新形式へ改名。更新前ファイルは同じ場所へ新形式・未使用の連番で保存。
- 通常ファイル・拡張子なしファイル・複数ドットのファイル・サブフォルダの除外を確認。
- 公開対象31ファイルを維持し、再生成したソースZIPとVSIXにバックアップが含まれないことを確認。
- 保存処理の既存テストは新旧のバックアップ名を検出するよう判定を更新。

## 0.1.0の表示名・バージョン・識別子の確認

- manifestで公開する全コマンドと実際の登録先、activationEventsとWebview登録先、診断名とパッケージ名を既存の模擬APIテストで照合。
- 新IDのコマンドからサンプル読込・図データ生成・GUI編集・SVG保存・既存 `.view.json` の読込／保存を検証。
- package.jsonとlockfileのプロジェクト版数、VSIXの版数がすべて `0.1.0` であることを確認。
- コマンドカテゴリ・Webviewタイトル・診断表示・Schema・開発用起動設定・説明文の公開名称を照合。
- 新VSIXのID `local.character-relationship-chart`、表示名「Character Relationship Chart」、バージョン0.1.0を展開して確認。
- 更新前のソース・配布物はバックアップとして保存し、公開対象と文字列検査の対象から除外。
- 旧ID側の内部保存データを新IDが自動で引き継ぐことは想定しない。切り替え前に旧版で表示ファイルへ保存する手順をREADMEへ記載。

## 未確認と理由

- デスクトップ版VS Code本体が実行環境にないため、VSIXの実インストール・F5起動・右クリックメニュー・Undoを未確認。
- ブラウザでWebviewを確認しようとしたが、ローカルファイルの表示がブラウザのセキュリティポリシーで拒否されたため、現行版のGUI操作・見た目・CSPの実ブラウザ検証は未実施。
- Windows、macOS、Remote／仮想ワークスペースでの実機試験は未実施。manifestの対応宣言と実機検証結果を区別する。
- 旧 `verification/1.2/` の画像・記録は今回の検証証拠として流用せず、公開対象から除外した。

現行版の画面例は [docs/images/README.md](docs/images/README.md) の手順で追加できます。

## 実機での最終確認

1. 開発版がある場合はREADMEの切り替え手順で表示データを保存し、必要に応じて無効化またはアンインストールしてから0.1.0のVSIXをインストールする。
2. 「Character Relationship Chart: サンプル設定を作成」で作業フォルダへ保存し、図が表示されることを確認する。
3. 任意名の `.jsonc` と従来の `.relations.json` で右クリックから図を開く。
4. GUIで人物・グループ・関係を追加・変更・削除する。複数所属、ID変更時の参照追従、セットの直線／曲線変更を確認する。
5. 設定を開いてコメント保持を確認し、Undo→保存→表示更新を確認する。
6. 人物をドラッグして拡大縮小し、隣の `.view.json` を確認する。図を閉じて開き直し、配置が復元されることを確認する。
7. 両ファイルを別フォルダへコピーして開く。表示中の設定をVS Codeで改名し、表示ファイルも追従することを確認する。
8. 他のエディターで設定・表示データを変更し、競合時の上書き停止を確認する。
9. SVG保存、検索、図だけ表示、狭い画面と明暗テーマを確認する。
10. 表示ファイルが壊れている場合、設定エラーの場合、書き込み権限がない場合に元データや未保存入力が失われないことを確認する。
