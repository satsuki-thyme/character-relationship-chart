# 検証結果と実機確認手順

## Web版転用・第3段階（2026-09-24 UTC）

対象: 共通コアと共有UIを使う読取専用Web版。保存・編集・Undo・競合管理・本格的なディレクトリ再編は対象外。ホストの提供機能に合わせてUIを制御し、VS Code側は従来の機能を維持する。反映記録は [stage3-applied.md](change-details/character-relationship-chart-stage3-applied.md)。

環境: Linux、Node.js v24.19.0、npm 11.9.0。新しい作業用コピーに依存を導入し、開発用esbuild 0.28.2だけを追加。本番jsonc-parser 3.3.1と既存依存の版数は維持した。jsdom 26.1.0、Playwright、Chromiumは検証専用の別フォルダーを使用。

| 項目 | 第3段階の結果 |
| --- | --- |
| 構文 | `npm run check` 成功。既存15＋Web・ビルド3の計18ファイル |
| 通常テスト | `npm test` 73件成功（第2段階66＋Webホスト7）、失敗・スキップなし |
| DOMテスト | VS Code版12件＋Web配布物6件、計18件成功、失敗・スキップなし |
| Webホスト | JSONC・BOM・表示データ、サイズ上限、不正入力・読込失敗時の保持、複数入力の一括検証、最後の読込要求だけの反映、購読解除・解放を検証 |
| Webの実配布コード | 一時出力したHTML・CSS・バンドルをfile URLからjsdomで読み、実FileReaderで入力を検証。VS Code API、編集処理、ネット通信、ブラウザ保存、ダウンロードAPIを使用しないことを確認 |
| 実ブラウザ | LinuxのChromium Headless 153.0.8010.0でオフライン起動。6人・8関係のサンプル、実ポインターでのドラッグとクリック、検索、詳細、ズームを確認 |
| ファイル操作 | ブラウザのファイル入力で任意名のJSONCと対応する表示データを読込。配置・カメラの復元、不正入力での保持、ドロップ、キャッシュ復帰相当のイベント後の読込、再読込でのサンプル復帰を確認 |
| 表示 | 1280×800と390×780でキャプチャを目視確認。日本語、詳細欄、図だけ表示、狭い画面で横方向にはみ出さないことを確認 |
| 通信・CSP | 通常操作中のHTTP(S)要求は0件。CSPでインラインスクリプトと接続が拒否されることをブラウザの違反イベントで確認。Webセキュリティを無効化するフラグは使用していない |
| Web配布 | `npm run build:web` 成功。ZIPは9ファイル。全エントリーが生成物と一致し、別の場所へ展開したZIPのindex.htmlでもオフライン起動を確認 |
| VSIX | `npm run package` 成功。53ファイル。実行コードと検証したソースが一致し、Web固有ファイル・ビルドツール・テストを除外。展開後のコアで解析・8本の経路計算も成功 |
| 互換性 | 共通コア5実装ファイルは第2段階と内容一致。データ形式、拡張ID・表示名・版数、本番依存の版数は変更なし |

| 配布物 | バイト数 | SHA-256 |
| --- | ---: | --- |
| `dist/character-relationship-chart-web-stage3.zip` | 41987 | `3eef71bcfed97559d1c38e4e2c2f4b089e32e0c5af6532352b1bf5250180adf5` |
| `dist/character-relationship-chart-stage3.vsix` | 423927 | `16007fb105a97f2296d228016be24bfbf1c62f0d8af5ba09b71538adfc444704` |

最初のWeb起動検証ではjsonc-parserのUMD入口にある動的requireが見つかったため、ビルドを公開ESM入口の選択に修正し、配布物の再検証に成功した。Playwright標準のブラウザ取得は破損したアーカイブで失敗したため、検証専用の別配布Chromiumを使用した。これらの検証用依存とフォントは配布物に含めていない。

未確認: Windows・macOS・Firefox・Safari、デスクトップ版VS Codeの実インストールとExtension Host・Undo。ブラウザの検証はLinuxの上記Chromiumであり、全OS・全ブラウザの動作確認ではない。VS Code側は既存の模擬API・DOM回帰検証まで。再実行方法は [開発ガイド](docs/DEVELOPMENT.md)、利用方法は [Web版README](web/README-ja.md) を参照。

## Web版転用・第2段階（2026-09-24 UTC）

対象: 第1段階のホスト境界を維持し、解析・編集・描画計算・表示データ検証を `packages/core` へ分離した版。拡張ID・表示名・版数、設定と表示データの形式、本番依存・lockfileは変更していない。クリックとドラッグの区別、エッジラベルの描画順も修正した。Dropboxへの反映記録は [stage2-applied.md](change-details/character-relationship-chart-stage2-applied.md) に記載する。

環境: Linux、Node.js v24.19.0、npm 11.9.0。第1段階と同じ固定済み依存を使用し、DOM検証用のjsdom 26.1.0はプロジェクト外に配置した。

| 項目 | 第2段階の結果 |
| --- | --- |
| 構文 | `npm run check` 成功。本番JavaScript15ファイル |
| 通常テスト | `npm test` 66件成功（第1段階61＋共通コア4＋既存表示ファイルの上限検証1）、失敗・スキップなし |
| 共通コア | 実際の `jsonc-parser` を含む依存グラフを、Node組込み・`process`・`Buffer`・DOM・VS Code APIのないVMで実行。設定の解析・編集、表示データの往復、描画計算を検証 |
| 共通描画スクリプト | CommonJSもDOMもない環境で `globalThis.RelationsGraph` として読み込めることを確認 |
| VS Code境界 | 実extension.jsと模擬VS Code APIでコアのリソース許可とスクリプト読込順を確認。既存の保存・競合・GUI編集・ID変更のテストも継続成功 |
| 表示データ | 厳密JSON、BOM、200,000文字上限、不明フィールド、特殊なキー、旧状態の検証を確認。上限を超えた既存表示ファイルは移行時にも保護し、旧保存状態を削除しない |
| DOM検証 | `test/ui-dom-check.js` 12件成功（第1段階7＋今回5）、失敗・スキップなし |
| ノード操作 | ドラッグで詳細を開かず、既存の選択も維持。移動後の保存・再読込、後続クリック、移動判定のしきい値以内のクリック、Enter/Space、`pointercancel` を検証 |
| ラベル表示 | ラベルと背景が全エッジ線の後に描画されるDOM順を確認。クリック、キーボード選択、選択の強調、検索、ドラッグ後の再描画も確認 |
| SVG出力 | XMLとして再解析でき、全エッジ線→全ラベル→ノードの順と矢印マーカーの参照を保つことを確認。重複した名前空間宣言も解消 |
| プレビュー配信 | `/vscode` と `/standalone` の2ページ・13アセットをHTTPで取得。共有描画スクリプトの内容と、standalone画面にVS Codeアダプターが含まれないことを確認 |
| VSIX | `npm run package` 成功。53ファイル、422,390バイト。実行コードとローカルソースの一致、共通コア・実行時依存・LICENSEの同梱、テスト・管理文書・変更記録・バックアップの除外を確認 |
| 配布物の動作 | 展開したVSIXから実コアを読み込み、実サンプルの解析・編集・表示データの往復・8本の経路計算、Webviewの参照先とラベル層を確認 |
| 移動した処理 | 設定解析・JSONC編集・描画計算の各実装、および表示データ正規化・エンコードの既存関数は、第1段階からの内容一致を確認 |

VSIX: `dist/character-relationship-chart-stage2.vsix`。SHA-256: `c9216e831f1c87dee31378c6724bfcf6634c1740a8c8b53ba27e36d0fcf25215`。拡張IDは `local.character-relationship-chart`、表示名は `Character Relationship Chart`、版数は `0.1.0`。

実ブラウザのエンジンとデスクトップ版VS Code本体が実行環境にないため、今回の検証はDOM・模擬API・配布物のコード実行まで。実ポインター入力、画面の見た目・テーマ・CSPの適用、VSIX実インストール、Extension Host、Undo、OS別動作は未確認。DOM検証では画面寸法、ポインターキャプチャ、一部描画タイミングを模擬している。末尾の実機確認手順を継続して使う。

独立Web版の起動・ファイル保存は後続段階の対象。検証の再実行方法は [開発ガイド](docs/DEVELOPMENT.md)、コアの契約は [日本語README](packages/core/README-ja.md) を参照。

## Web版転用・第1段階（2026-09-23 UTC）

対象: 0.1.0を基にしたUI/VS Code境界分離版。拡張ID・表示名・版数、データ形式は変更していない。作業用コピーで実装・検証し、更新ファイルとVSIXを生成。以下は直接反映前に取得した検証結果。commit/push、公開は行っていない。Dropboxへの反映記録は [stage1-applied.md](dist/character-relationship-chart-stage1-applied.md) に記載する。

環境: Linux、Node.js v24.19.0、npm 11.9.0。Dropboxの現行ソースはダウンロード時にサイズと内容ハッシュを検証して取得した。

| 項目 | 今回の結果 |
| --- | --- |
| 変更前の基準 | 既存54テスト成功 |
| 依存導入 | lockfileを維持して `npm ci --ignore-scripts` 成功 |
| 構文 | `npm run check` 成功。本番JavaScript10ファイル |
| 通常テスト | `npm test` 61件成功（既存54＋境界7）、失敗・スキップなし |
| 境界 | API取得・旧状態消去をアダプターへ隔離。通信順、購読解除、非同期応答の対応付け、破棄時の未完了要求を検証 |
| 拡張との接続 | 実アダプター＋実extension.js＋模擬VS Code APIでGUI編集・人物ID変更と配置引継ぎ・版競合・保存失敗・SVG保存を検証 |
| DOM検証 | jsdom 26.1.0の追加7件成功。VS Code APIなしのホスト注入、描画・編集、コメント保持、配置保存/再読込、設定を開く操作、SVG生成、入力保持、古い保存応答、新しい外部変更、不正設定・不正メッセージ・HTML文字列を確認 |
| 依存関係 | 本番の依存・lockfileは変更なし。jsdomは検証用の別フォルダへ導入し、VSIXに含めない |
| VSIX | `npm run package` 成功。共有HTML、新しいアダプター・起動スクリプト・テーマCSS、jsonc-parserとLICENSEを含む。開発テスト・管理文書・バックアップを除外 |
| 維持した処理 | `src/extension.js`、`storage.js`、`config.js`、`edit.js`、`media/graph.js`、Schema、サンプルは取得時から変更していない |

実ブラウザによる画面確認は、提供されたブラウザがローカル検証URLを `ERR_BLOCKED_BY_CLIENT` で拒否したため未実施。ローカルChromiumの起動試行もプロセスが終了した。DOM検証では画面サイズ・ResizeObserver・ダイアログ・一部描画タイミングを模擬しているため、見た目、ドラッグの実操作、明暗テーマの色、CSPの実ブラウザでの適用を確認したとは扱わない。

実際のVS Code本体がないため、VSIX実インストール、Extension Host、Undo、OS別動作は未確認。末尾の実機確認手順を継続して使う。独立Web製品版は未実装。検証用画面とDOM検証の再実行方法は [開発ガイド](docs/DEVELOPMENT.md) を参照。

## 以下は公開準備時の記録（今回の再実行結果とは別）

対象: Character Relationship Chart 0.1.0（2026-09-23）。Linux、Node.js v24.19.0、npm 11.9.0で確認。

## この版で確認したこと

| 項目 | 結果・範囲 |
| --- | --- |
| クリーン導入 | 公開対象31ファイルのみを別ディレクトリへ展開し、既存node_modulesやGitメタデータなしで `npm ci` →構文検査→54テスト→VSIX作成に成功 |
| JavaScript構文 | `npm run check` 成功（src／mediaの8ファイル） |
| 既存の自動テスト | `npm test` 54件成功、失敗・スキップなし |
| テスト内訳 | 設定14、JSONC編集7、VS Code模擬API12、描画計算12、保存9 |
| 実ファイルの読み書き | 別の一時フォルダへサンプルを保存し、人物ID・複数所属の変更、関係参照の追従、コメント保持を確認 |
| 配置データの保存 | 本番のcreateStorageへ実ファイルAPIを渡し、書込・再読込・別名／別フォルダへの移動後の復元を確認 |
| 保存競合 | 外部で書き換えた表示ファイルを保護し、古い状態での上書きを拒否 |
| サンプルと描画計算 | 6人物・4グループ・8関係の読込と8本の経路計算が成功 |
| VSIX | `npm run package` 成功。実行時依存・サンプル・Schema・GUIスクリプト・第三者LICENSEを同梱 |
| 公開範囲 | 整備用バックアップは新形式 `backup-YYYY-MM-DD-NN` に統一し、新旧両形式がGitで除外。VSIXにもバックアップ・verification・開発テスト・個人用フォルダなし |
| 内容の確認 | 公開対象ソースと新しいソースZIP・VSIXの内容・ファイル名に、廃止した個人用名称がないことを大文字小文字を区別せず確認 |
| Git履歴 | 初回公開準備時の調査では初回コミット前。今回もcommit・pushは実行していない |

VS Code模擬APIのテストは実際のExtension HostやWindowsのGUIを実行するテストではありません。描画計算の成功も画面の見た目を保証するものではありません。

## バックアップ規則更新の確認

- 既存13バックアップは内容を変えずに新形式へ改名。更新前ファイルは同じ場所へ新形式・未使用の連番で保存。
- 通常ファイル・拡張子なしファイル・複数ドットのファイル・サブフォルダの除外を確認。
- 公開対象31ファイルを維持し、再生成したソースZIPとVSIXにバックアップが含まれないことを確認。
- 保存処理の既存テストは新旧のバックアップ名を検出するよう判定を更新。

## 0.1.0の表示名・バージョン・識別子の確認

- manifestで公開する全コマンドと実際の登録先、activationEventsとWebview登録先、診断名とパッケージ名を既存の模擬APIテストで照合。
- 新IDのコマンドからサンプル読込・図データ生成・GUI編集・SVG保存・既存 `.view.json` の読込／保存を検証。
- package.jsonとlockfileのプロジェクト版数、VSIXの版数がすべて `0.1.0` であることを確認。
- コマンドカテゴリ・Webviewタイトル・診断表示・Schema・開発用起動設定・説明文の公開名称を照合。
- 新VSIXのID `local.character-relationship-chart`、表示名「Character Relationship Chart」、バージョン0.1.0を展開して確認。
- 更新前のソース・配布物はバックアップとして保存し、公開対象と文字列検査の対象から除外。
- 旧ID側の内部保存データを新IDが自動で引き継ぐことは想定しない。切り替え前に旧版で表示ファイルへ保存する手順をREADMEへ記載。

## 未確認と理由

- デスクトップ版VS Code本体が実行環境にないため、VSIXの実インストール・F5起動・右クリックメニュー・Undoを未確認。
- ブラウザでWebviewを確認しようとしたが、ローカルファイルの表示がブラウザのセキュリティポリシーで拒否されたため、現行版のGUI操作・見た目・CSPの実ブラウザ検証は未実施。
- Windows、macOS、Remote／仮想ワークスペースでの実機試験は未実施。manifestの対応宣言と実機検証結果を区別する。
- 旧 `verification/1.2/` の画像・記録は今回の検証証拠として流用せず、公開対象から除外した。

現行版の画面例は [docs/images/README.md](docs/images/README.md) の手順で追加できます。

## 実機での最終確認

1. 開発版がある場合はREADMEの切り替え手順で表示データを保存し、必要に応じて無効化またはアンインストールしてから0.1.0のVSIXをインストールする。
2. 「Character Relationship Chart: サンプル設定を作成」で作業フォルダへ保存し、図が表示されることを確認する。
3. 任意名の `.jsonc` と従来の `.relations.json` で右クリックから図を開く。
4. GUIで人物・グループ・関係を追加・変更・削除する。複数所属、ID変更時の参照追従、セットの直線／曲線変更を確認する。
5. 設定を開いてコメント保持を確認し、Undo→保存→表示更新を確認する。
6. 人物をクリックすると詳細が開き、ドラッグでは詳細が開かず既存の選択も変わらないことを確認する。拡大縮小し、隣の `.view.json` を確認する。図を閉じて開き直し、配置が復元されることを確認する。
7. 両ファイルを別フォルダへコピーして開く。表示中の設定をVS Codeで改名し、表示ファイルも追従することを確認する。
8. 他のエディターで設定・表示データを変更し、競合時の上書き停止を確認する。
9. SVG保存、検索、図だけ表示、狭い画面と明暗テーマを確認する。複数のエッジがラベル位置を横切る配置でも、全ラベルとその背景が全エッジ線より前に描画されることを画面と保存SVGで確認する。
10. 表示ファイルが壊れている場合、設定エラーの場合、書き込み権限がない場合に元データや未保存入力が失われないことを確認する。
