# 更新履歴

## Unreleased — Web版転用の第3段階

- 共通コアと共有UIを使う読取専用Webビューアーを追加。JSONC / JSONと対応する表示データをファイル選択・ドロップで読み込む。
- 検索・詳細・拡大縮小・ドラッグを提供し、編集・保存・SVG出力・ブラウザ保存領域は実装しない。配置は一時的な表示状態として扱う。
- ホストのcapabilitiesで利用できる操作を制御。省略時は従来のVS Code操作を維持する。
- 別の設定へ切り替えると選択・検索・配置をリセット。不正入力・読込失敗では直前の図を保持し、古い読込結果の後着を無視する。
- 開発用esbuild 0.28.2と再現可能な静的Webビルドを追加。既存依存の版数・本番依存・データ形式・拡張ID・版数は維持。
- 保存・編集・Undo・競合管理と、本格的なディレクトリ再編は後続段階に残す。

### 第2段階の記録

- 解析・検証・JSONC編集・表示データ・描画計算を `packages/core/` へ集約。VS Code側は共通コアを呼び、既存の保存・Undo・競合処理を維持。
- ノードのドラッグで詳細欄を開かず、現在の選択・詳細を維持。クリック・キーボード操作では従来どおり詳細を表示。
- 全エッジの線・矢印より前にラベルを描画。再描画・検索・選択とSVG出力でも同じ順序を維持。
- SVGの重複した名前空間宣言を避け、出力をXMLとして再読込する検証を追加。
- coreのホスト非依存性、表示データの上限と移行保護、UI操作・SVGの回帰検証を追加。
- 版数・識別子は0.1.0のまま。独立Web版は未提供。

### 第1段階

- 共有UIにホストを注入し、VS Codeの通信・起動・テーマ処理をアダプターへ分離。
- 共通HTMLを独立。既存のJSONC・表示データ・保存・競合検知を維持。
- 境界とGUIの入力保持の検証、ローカル検証画面を追加。
- 拡張の版数・識別子は0.1.0のまま。独立Web版は未提供。

## 0.1.0 — 2026-09-23

初回公開版。公開前の開発用の版数を整理し、0.1.0から開始します。

- 表示名を「Character Relationship Chart」に統一。拡張機能IDは `local.character-relationship-chart`、コマンドは `characterRelationshipChart.*`。
- JSON / JSONCによる人物・グループ・関係の管理、任意名の `.jsonc` からの相関図表示に対応。
- キャラクターの複数グループ所属、矢印・線種・直線／曲線・並行線セットを設定可能。
- GUIによる人物・グループ・関係・全体設定の編集、コメント保持、ID変更時の参照追従、外部編集との競合検知に対応。
- 自動配置・円形配置、ドラッグ、拡大縮小、検索、詳細表示、図だけ表示、SVG保存に対応。
- 配置や倍率を設定ファイルの隣の `.view.json` に保存。旧内部データのファイル移行処理を用意。拡張自身は保存時のバックアップを作成しない。
- 公開用サンプル、JSON Schema、導入・開発・データ仕様・配布手順、MIT Licenseと第三者ライセンスを整備。
- 整備時のバックアップ、生成物、個人データ、ローカル設定を公開対象から除外。
